Automated UI tests for a patient dashboard using Playwright and Pytest

Installing dependencies
pip install pytest playwright
playwright install
pip install pytest

To run the test:
pytest tests/ --headed


